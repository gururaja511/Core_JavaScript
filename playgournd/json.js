var jsonUtil=require('./jsonUtil.js');

/*var obj={
  name:"GuruRaja"
};
var stringObj=JSON.stringify(obj);
console.log(typeof stringObj);
console.log(obj);
console.log(stringObj.name);
console.log(obj.name);
console.log("conver the string into json and retrive name "+JSON.parse(stringObj).name);

var parseJson='{"name":"GuruRaja","age":"28"}';
console.log(JSON.parse(parseJson).age);
*/
/*
const fs=require('fs');
var orginalNote={
  title:'some note',
  bodey:'some body'
};
fs.writeFileSync("jsonFile.json",JSON.stringify(orginalNote));

var noteString=fs.readFileSync("jsonFile.json");

console.log(JSON.parse(noteString).title);
*/
//var notes=jsonUtil.getNotes();
//console.log(notes);
// notes.push({'hi','by'})
//jsonUtil.saveNotes(notes);

//jsonUtil.addNote('some note3','hello');
//jsonUtil.removeNode('some note');
//jsonUtil.update('some note2','updatedbody2');
//console.log(jsonUtil.getNode('some note2').body);
// for array we can use forEach() or filter();
/*var allNodes=jsonUtil.getNodes();
var i=0;
allNodes.forEach((node)=>{
  console.log(node);
  i++;
  console.log(i);

});*/




















//
